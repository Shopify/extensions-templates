/// <reference types="@shopify/ui-extensions/admin.product-variant-purchase-option.action.render" />
import {render} from 'preact';
import PurchaseOptionsActionExtension from './PurchaseOptionsActionExtension';

export default function extension() {
  console.log(shopify.extension.target);
  render(<PurchaseOptionsActionExtension />, document.body);
}
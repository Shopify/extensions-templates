api_version = "2026-07"

[[extensions]]
type = "ui_extension"
name = "t:name"
{% if uid %}uid = "{{ uid }}"{% endif %}
handle = "{{ handle }}"
description = "A {{ flavor }} POS background extension"

[[extensions.targeting]]
module = "./src/BackgroundExtension.{{ srcFileExtension }}"
target = "pos.app.ready.data"

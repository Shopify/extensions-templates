description = "Your description"

[[extensions]]
name = "{{ name }}"
type = "flow_trigger"
handle = "{{ handle }}"

[settings]

  [[settings.fields]]
  type = "customer_reference"

  [[settings.fields]]
  type = "single_line_text_field"
  key = "your-field-key"
